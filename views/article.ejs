<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %></title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/comments.css">

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-2S2ZGQ6F6H"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-2S2ZGQ6F6H');
    </script>
</head>
<body>
    <header>
        <div class="color-block">
            <img src="/logo.png" alt="Women's Football Logo" class="logo">
            <h1>Women's Football News</h1>
        </div>
        <nav>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/articles/list">Articles</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <article>
            <h1><%= article.title %></h1>
            <img src="<%= article.image %>" alt="<%= article.title %>" class="article-image">
            <p>Author: Panashe Hove</p>
            <div style="white-space: pre-wrap;">
                <%= article.content %>
            </div>
            <% if (article.image) { %>
                <img src="<%= article.image %>" alt="<%= article.title %>" class="article-image">
            <% } %>
            <!-- Like and Dislike Buttons -->
            <button class="like-button" data-id="<%= article._id %>" onclick="likeArticle('<%= article._id %>')" style="background-color: <%= article.liked ? 'black' : 'white' %>;">👍 (<%= article.likes %>)</button>
            <button class="dislike-button" data-id="<%= article._id %>" onclick="dislikeArticle('<%= article._id %>')" style="background-color: <%= article.disliked ? 'black' : 'white' %>;">👎 (<%= article.dislikes %>)</button>
        </article>

        <!-- Comment Section -->
        <div class="comment-section">
            <div class="comments-wrp"></div> <!-- Comments Wrapper -->
            <div class="reply-input container">
                <img src="/images/avatars/image-juliusomo.webp" alt="" class="usr-img">
                <textarea id="new-comment" class="cmnt-input" placeholder="Add a comment..."></textarea>
                <button class="bu-primary" onclick="submitComment('<%= article._id %>')">SEND</button>
            </div> <!-- Reply Input -->
        </div> <!-- Comment Section -->

        <div class="modal-wrp invisible">
            <div class="modal container">
                <h3>Delete comment</h3>
                <p>Are you sure you want to delete this comment? This will remove the comment and can't be undone.</p>
                <button class="yes">YES, DELETE</button>
                <button class="no">NO, CANCEL</button>
            </div>
        </div>
    </main>
    <script src="/script.js"></script>
    <script src="/comments.js"></script>
</body>
</html>
