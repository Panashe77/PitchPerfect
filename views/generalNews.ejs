<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>General News</title>
    <link rel="stylesheet" href="/style.css">
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-2S2ZGQ6F6H"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-2S2ZGQ6F6H');
    </script>
</head>
<body>
    <header>
        <div class="color-block">
            <img src="/logo.png" alt="Women's Football Logo" class="logo">
            <h1>Women's Football News</h1>
        </div>
        <nav>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/articles/list">Articles</a></li>
                <li><a href="/articles/general-news">General News</a></li>
            </ul>
            <% if (!loggedIn) { %>
                <a href="/users/login" class="login-link">Login</a>
            <% } else { %>
                <a href="/users/profile" class="profile-link"><img src="/user-profile.png" alt="Profile" class="profile-icon"></a>
            <% } %>
        </nav>
    </header>
    <main>
        <section id="generalNews">
            <h2>General News</h2>
            <ul>
                <% articles.forEach(function(article) { %>
                    <li>
                        <h2><a href="/articles/<%= article.id %>"><%= article.title %></a></h2>
                        <% if (article.image) { %>
                            <a href="/articles/<%= article.id %>">
                                <img src="<%= article.image %>" alt="<%= article.title %>" class="article-thumbnail">
                            </a>
                        <% } %>
                    </li>
                <% }); %>
            </ul>
        </section>
    </main>
    <footer>
        <p>© 2024 Women's Football News</p>
    </footer>
</body>
</html>
